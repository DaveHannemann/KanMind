services:
  backend:
    build: .
    container_name: django_backend
    ports:
      - "8000:8000"

    env_file:
      - .env
      
    volumes:
      - ./static:/usr/src/app/static
      # - ./db.sqlite3:/usr/src/app/db.sqlite3